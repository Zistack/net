int
parse (T * config, std::string config_filename)
{
	// We gots to parse a file now.  What fun.  I need to decide on a
	// nonterrible syntax for this, and grab me a lexer that doesn't suck.

	// We could use our JSON library.  That would actually make sense.
}
